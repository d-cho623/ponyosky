<header class="second-header">
  <%= link_to "PONYOSKY" ,"/",class: "second-h1"%>
</header>

<%= form_with model: @user, url: user_registration_path, class: 'registration-main', local: true do |f| %>

  <div class="content">
    <h1>新規ユーザー登録</h1>
    <%= render "devise/shared/error_messages", resource: resource %>
    <p>次のフォームにユーザー情報を入力してください。</p>
    <div class="control">
      <label for="uid">ユーザーID <span class="required">必須</span></label>
      <%= f.text_field :uid, class:"input-default", id:"userid", placeholder:"例) ponyosky", maxlength:"40" %>
    </div>
    <div class="control">
      <label for="name">名前 <span class="required">必須</span></label>
      <%= f.text_field :name, class:"input-default", id:"username", placeholder:"例) ぽにょ太郎", maxlength:"40" %>
    </div>
    <div class="control">
      <label for="mymail">メールアドレス <span class="required">必須</span></label>
      <%= f.email_field :email, class:"input-default", id:"email", placeholder:"PC・携帯どちらでも可", autofocus: true %>
    </div>
    <div class="control">
      <label for="password">パスワード <span class="required">必須</span></label>
      <%= f.password_field :password, class:"input-default", id:"password", placeholder:"6文字以上の半角英数字" %>
    </div>
    <div class="control">
      <label for="password">パスワード(確認) <span class="required">必須</span></label>
      <%= f.password_field :password_confirmation, class:"input-default", id:"password-confirmation", placeholder:"同じパスワードを入力して下さい" %>
    </div>
    <div class="control">
      <label for="occupation">役職 <span class="required">必須</span></label>
      <%= f.collection_select(:occupation_id, Occupation.all, :id, :name, {}, {class:"select-box"}) %>
    </div>
    
    <div class="control">
      <label for="workplace">職場 <span class="no-required">任意(所属している場合は選択してください)</span></label>
      <%= f.collection_select(:workplace_id, Workplace.all, :id, :name, {}, {class:"select-box"}) %>
    </div>
    <div class="control">
      <label for="group">組 <span class="no-required">任意(所属している場合は選択してください)</span></label>
      <%= f.collection_select(:group_id, Group.all, :id, :name, {}, {class:"select-box"}) %>
    </div>
    <div class="control">
      <button type="submit">登録する</button>
    </div>
  </div>

<% end %>
<%# <%= render "devise/shared/links" %>
